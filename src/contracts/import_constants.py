"""Shared constants for import validation."""

from __future__ import annotations

# Supported file extensions for document import (case-insensitive)
SUPPORTED_EXTENSIONS = frozenset({".epub", ".pdf", ".txt", ".md"})
